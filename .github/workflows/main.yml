on: [push]

jobs:
    appium_job:
        runs-on: ubuntu-latest
        name: Run Appium Test
        steps:
        - name: Appium Test step
          id: Appium
          uses: Zemotacqy/Appium-js@v1
          env: 
            bs_username: ${{ secrets.bs_username }}
            bs_accesskey: ${{ secrets.bs_accesskey }}  
